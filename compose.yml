services:
  frontend:
    image: ghcr.io/rgss-cs/frontend:latest
    container_name: frontend
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - internetwork
    pull_policy: always

networks:
  internetwork:
    driver: bridge
