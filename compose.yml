services:
  frontend:
    image: ghcr.io/rgss-cs/frontend:latest
    container_name: frontend
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - internetwork
    pull_policy: always

networks:
  internetwork:
    driver: bridge